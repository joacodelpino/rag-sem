# torch NO va acá: se instala aparte con el índice CPU-only de PyTorch para
# no bajar los ~2 GB de wheels con CUDA que no vamos a usar. Ver README.
streamlit>=1.38
qdrant-client>=1.11
sentence-transformers>=3.1
PyMuPDF>=1.24
openai>=1.47
python-dotenv>=1.0

# Evaluación. ragas 0.4.3 hace `from langchain_community.chat_models.vertexai
# import ChatVertexAI`, ruta que langchain-community 0.4 eliminó: sin el tope
# de abajo, `import ragas` revienta con ModuleNotFoundError. No es un capricho
# de versión, es la única combinación que importa.
ragas>=0.4,<0.5
langchain-community<0.4
